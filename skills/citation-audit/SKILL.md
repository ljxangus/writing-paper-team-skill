---
name: citation-audit
description: Use when auditing citations in research papers - implements Audit Agent Stage 6 workflow with DOI verification, format consistency checking, and five-step validation gates
# Hermes Agent
tools: [bash, read, write, edit]
# WorkBuddy MCP
mcp_servers: [citation_verify]
# Claude Code
subagent_types: [audit]
---

# Citation Audit — 引用审核技能

独立的引用审核技能，基于 Audit Agent Stage 6 工作流程，执行引用准确性验证、格式一致性检查和五步验证门控。

---

## 核心原则

1. **独立客观** — 审查结果基于证据，不受其他 Agent 影响
2. **逐项验证** — 每项检查必须有明确的通过/不通过判定和证据
3. **建设性反馈** — 不通过时给出具体的问题描述和修改建议
4. **不可妥协** — 审查标准不可因"差不多"而降低

---

## 引用审核流程

### Step 1: 收集全部引用

读取所有 `chapters/*.md`，提取：
- 正文中的所有引用标记（如 [1], [2], [Author et al., Year] 等）
- 参考文献列表中的所有条目

### Step 2: 逐项审核

#### 2.1 DOI 有效性验证

使用 CrossRef API 批量验证：

```bash
python3 scripts/citation_verify.py --input papers/references.bib --check-doi --output audit/citation-doi-report.json
```

或手动验证：
```
GET https://api.crossref.org/works/{doi}
```

| 检查项 | 标准 |
|--------|------|
| DOI 格式 | 符合 DOI 标准（10.xxxx/xxxx） |
| DOI 可访问 | CrossRef API 返回 200 |
| DOI 对应内容 | 标题、作者与引用信息一致 |

#### 2.2 引用信息准确性

| 检查项 | 标准 |
|--------|------|
| 作者姓名 | 拼写正确，顺序与原文一致 |
| 发表年份 | 与实际发表年份一致 |
| 标题 | 与原文标题完全一致 |
| 期刊/会议名 | 名称准确，缩写规范 |
| 卷/期/页码 | 与原文一致 |

#### 2.3 格式一致性

| 检查项 | 标准 |
|--------|------|
| 引用风格 | 全文统一（IEEE [1] 或 ACM [1]） |
| 正文格式 | 引用编号格式一致 |
| 文献列表格式 | 每条引用的字段顺序和标点一致 |
| 必填字段 | 每条引用至少包含：作者、标题、年份、出处 |

#### 2.4 正文-文献列表对应

| 检查项 | 标准 |
|--------|------|
| 正文引用有对应条目 | 正文中每个 [N] 在文献列表中有条目 |
| 文献列表有正文引用 | 文献列表中每条都在正文中被引用 |
| 编号连续 | 引用编号从 [1] 开始连续无跳号 |
| 顺序一致 | 首次引用顺序与编号一致（如采用顺序编码制） |

### Step 3: 生成引用审核报告

输出到 `audit/citation-audit-report.md`：

```markdown
# Citation Audit Report

**审查日期**: YYYY-MM-DD
**审查范围**: 全部 chapters/*.md + 参考文献列表
**审查结论**: ✅ PASS / ❌ FAIL

## 审查摘要

- 总引用数: [N]
- DOI 验证通过: [M/N]
- 信息准确: [K/N]
- 格式一致: 是/否
- 正文-文献列表对应: 是/否

## 逐条审核结果

| # | 引用 | DOI | 信息准确 | 格式 | 正文对应 | 问题 |
|---|------|-----|----------|------|----------|------|
| 1 | Author et al., "Title"... | ✅ | ✅ | ✅ | ✅ | - |
| 2 | Author et al., "Title"... | ❌ | - | - | - | DOI 404 |

## 问题清单

### Critical
1. [引用N]: [问题描述]

### Major
1. [引用N]: [问题描述]

### Minor
1. [引用N]: [问题描述]
```

### Step 4: 审查判定

- **✅ PASS**：所有引用 DOI 有效、信息准确、格式统一、正文-文献列表对应
- **❌ FAIL**：存在任何 DOI 无效、信息错误或格式不一致

审查不通过时，Writing Agent 修正后重新提交引用终审。

---

## 验证门控（五步法）

> **声称完成而没有验证，是不诚实的表现。**
> **没有验证证据，就不能声称完成。**

在声称任何审查结论之前，必须执行以下五步：

### Step 1: 确认 — 什么命令/操作能证明这个声称？

### Step 2: 执行 — 运行完整的验证操作

- 引用审查：调用 CrossRef API、搜索原始来源、运行 `scripts/citation_verify.py`

### Step 3: 读取 — 完整输出，检查结果

### Step 4: 验证 — 输出是否确认声称？

- 如果**否**：说明实际状态和证据
- 如果**是**：说明声称和证据

### Step 5: 然后才能 — 做出声称

⚠️ **跳过任何步骤 = 撒谎，不是验证**

### 常见验证场景

| 声称 | 需要的验证 | 不充分 |
|------|----------|--------|
| 引用全部有效 | DOI 批量验证通过，信息匹配 | "引用看起来正确" |
| 格式无问题 | 运行 style_check.sh 和格式检查 | "格式应该没问题" |
| 数据一致性确认 | 论文数值与实验输出逐条比对 | "数据差不多" |

---

## 通用审查纪律

### 不可接受的做法（借口与事实对照）

| 借口 | 事实 |
|------|------|
| "看起来没问题" | 必须逐项验证，运行验证命令 |
| "应该是对的" | 必须有证据支持 |
| "就差一点" | 标准不可降级 |
| "太麻烦了跳过" | 不可跳过任何检查项 |
| "我很确信" | 确信 ≠ 证据 |
| "就这一次" | 没有例外 |
| "部分检查够了" | 部分证明不了什么 |
| "代理说成功了" | 独立验证 |

### 必须遵守的规则

- 每项检查有明确的 ✅ 或 ❌ 判定
- 不通过必须给出具体问题描述和修改建议
- 审查报告必须包含完整的逐项结果
- 审查报告保存到指定路径
- 验证结果必须在报告中呈现（而非仅口头声称）

---

## 审查完成自检

- [ ] 运行了验证操作（API 调用 / 脚本运行）
- [ ] 读取了完整验证输出
- [ ] 确认结果支持审查结论
- [ ] 没有依赖"应该"或"可能"
- [ ] 证据在审查报告中呈现

---

## 相关参考

- 完整 Agent 定义：`references/agent-audit.md`
- 验证脚本：`scripts/citation_verify.py`
- 审稿评分标准：`references/ieee-acm-rubric.md`
